#include <iostream>
#include <windows.h>
#include <cmath>
using namespace std;

main()
{
    int size;
    cout<<"Enter size of the array : ";
    cin>>size;
    int number














    int number[5];
    for(int idx = 0; idx<5; idx++)
    {

    cin>>number[idx];
    }
    for (int idx = 0;idx<5;idx++)
    {
        cout<<" "<<number[idx];
    }
}